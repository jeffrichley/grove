### Python Workflow

- Load `.grove/rules/python.md` when working in `src/` or `tests/`.
- Prefer `uv run pytest` or `just test` for test execution.
- Keep Python changes typed and validated with Ruff and mypy.
